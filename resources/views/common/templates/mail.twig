<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8" />
        <title>{{ mail.subject }}</title>
    </head>
    <body>
        <h1>{{ mail.subject }}</h1>
        <span>{{ mail.body }}</span>
    </body>
</html>
